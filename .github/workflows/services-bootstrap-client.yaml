# THIS FILE IS GENERATED AUTOMATICALLY BY RUNNING gen-workflow.sh 
# DON'T CHANGE IT MANUALLY TO AVOID YOUR CHANGES BEING OVERWRITTEN
# USE workflow-template.yaml FOR MAKING CHANGES IN WORKFLOWS
name: build-client
on:  
  push:   
    paths:
        - "services/bootstrap/client/**"
        - ".github/workflows/services-bootstrap-client.yaml"
    tags:
        - "*"
  workflow_dispatch:

jobs:  
  build:
    env: 
      working-directory: services/bootstrap/client

    name: build and test
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3

    - name: Set up Go
      uses: actions/setup-go@v2
      with:
        go-version: 1.18
  
    - name: Test
      run: go test -v ./... -coverprofile="coverage.out"
      working-directory: ${{ env.working-directory }}
    
  lint:
    name: lint
    runs-on: ubuntu-latest
    env: 
      working-directory: services/bootstrap/client
    steps:
      - uses: actions/checkout@v3
      - name: lint
        uses: ./.github/actions/lint-go
        with:
          working-directory: ${{ env.working-directory }}
          github-bot-token: ${{ secrets.UKAMA_BOT_GITHUB_TOKEN }}
          