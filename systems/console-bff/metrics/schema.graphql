type Query {
  getMetrics(input: GetMetricInput!): [Metric!]!
}

type Subscription {
  getMetricEvent(input: GetMetricInput!): Metric!
}

type Metric {
  value: String
}

input GetMetricInput {
  userId: String
  orgId: String
  type: String
}
