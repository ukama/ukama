--- a/testing/lab/inc/bff.h
+++ b/testing/lab/inc/bff.h
@@ -145,6 +145,9 @@
 
 typedef struct {
     char status[ULAB_MAX_REF];
+    /* Value of the row's "active" attribute ("true"/"false"). Distinct from
+     * status, which is a sales-performance label, not the plan flag. */
+    char active[ULAB_MAX_REF];
     uint32_t row_index;
     uint32_t row_count;
     int has_name;
--- a/testing/lab/inc/scenario.h
+++ b/testing/lab/inc/scenario.h
@@ -157,6 +157,7 @@
     char         ref[ULAB_MAX_REF];
     char         entity[ULAB_MAX_REF];
     char         status[ULAB_MAX_REF];
+    char         expected_active[ULAB_MAX_REF];
     char         expected[ULAB_MAX_REF];
     char         app[ULAB_MAX_NAME];
     char         key[ULAB_MAX_REF];
--- a/testing/lab/src/bff.c
+++ b/testing/lab/src/bff.c
@@ -2373,7 +2373,11 @@
             if (ulab_streq(key, "name")) row->has_name = 1;
             else if (ulab_streq(key, "price")) row->has_price = 1;
             else if (ulab_streq(key, "validity")) row->has_validity = 1;
-            else if (ulab_streq(key, "active")) row->has_active = 1;
+            else if (ulab_streq(key, "active")) {
+                row->has_active = 1;
+                json_get_optional_str(attribute, "value", row->active,
+                                      sizeof(row->active));
+            }
         }
         *found = 1;
         break;
--- a/testing/lab/src/scenario.c
+++ b/testing/lab/src/scenario.c
@@ -499,6 +499,8 @@
         sizeof(c->entity), val);
     if (ulab_streq(key, "status")) return ulab_copy(c->status,
         sizeof(c->status), val);
+    if (ulab_streq(key, "active")) return ulab_copy(c->expected_active,
+        sizeof(c->expected_active), val);
     if (ulab_streq(key, "app")) return ulab_copy(c->app,
         sizeof(c->app), val);
     if (ulab_streq(key, "key")) return ulab_copy(c->key,
--- a/testing/lab/src/check_analytics.c
+++ b/testing/lab/src/check_analytics.c
@@ -9,6 +9,7 @@
 #include <math.h>
 #include <stdio.h>
 #include <string.h>
+#include <strings.h>
 #include <time.h>
 #include <unistd.h>
 
@@ -417,6 +418,9 @@
         if (matched && check->status[0] != '\0') {
             matched = ulab_streq(row.status, check->status);
         }
+        if (matched && check->expected_active[0] != '\0') {
+            matched = strcasecmp(row.active, check->expected_active) == 0;
+        }
         memset(&other_row, 0, sizeof(other_row));
         other_found = 0;
         if (matched && other != NULL &&
@@ -439,12 +443,12 @@
     res->passed = matched;
     snprintf(res->detail, sizeof(res->detail),
              "report=%.40s package=%.40s found=%s row=%u/%u status=%.16s "
-             "attributes=%d/%d/%d/%d other_found=%s other_row=%u",
+             "active=%.8s attributes=%d/%d/%d/%d other_found=%s other_row=%u",
              check->report, check->package_ref,
              found ? "true" : "false", row.row_index, row.row_count,
-             row.status, row.has_name, row.has_price, row.has_validity,
-             row.has_active, other_found ? "true" : "false",
-             other_row.row_index);
+             row.status, row.active, row.has_name, row.has_price,
+             row.has_validity, row.has_active,
+             other_found ? "true" : "false", other_row.row_index);
     return ULAB_OK;
 }
 
--- a/testing/lab/docs/scenario-language-v1.md
+++ b/testing/lab/docs/scenario-language-v1.md
@@ -378,7 +378,28 @@
 - `kpi_contract` can require `expected_partial`, `computedAt`, scope, and
   trend consistency. `kpi_rollup_consistency` compares daily, weekly, and
   monthly values. `performance_report_row` verifies row identity attributes,
-  status, and optional before/after ordering.
+  status, the plan `active` attribute, and optional before/after ordering.
+
+`performance_report_row` distinguishes two fields that are easy to confuse:
+
+- `status:` matches the row's sales-performance label. For
+  `package_performance` the server vocabulary is `Inactive`, `No sales`,
+  `Low sales`, `Active`, computed from the report spec's status rules
+  (`active == false` -> `Inactive`, `sold == 0` -> `No sales`, `sold < 25` ->
+  `Low sales`, otherwise `Active`). The comparison is case sensitive, and
+  `Active` means "active plan with enough sales", not "the plan flag is on".
+- `active:` matches the row's `active` attribute (`true`/`false`), which is the
+  plan flag from the catalog. The comparison is case insensitive.
+
+Monetary cells in `performance_report_cell` follow the `unit` the server
+returns. `package_performance.revenue` is `cents`, so a 7.00 USD plan sold once
+is `expected_value: 700`. This differs from `revenue_summary` and
+`package_business_metrics`, which read `commerceView` and are in major units.
+
+Note that `span:` is inert for `package_performance`: the aggregator accepts
+`daily|weekly|monthly` but the composer only honours the rolling tokens
+(`last_24h`, `last_7d`, `last_30d`). Any calendar span falls back to the
+configured report window and the response echoes that window's label (`8w`).
 
 `status_equals` for SIMs supports `active` and `inactive` based on active
 package assignment. `set_sim_status` only validates the mutation path in this
--- a/testing/lab/scenarios/p0/console-kpi/package/package-performance-report.yaml
+++ b/testing/lab/scenarios/p0/console-kpi/package/package-performance-report.yaml
@@ -1,6 +1,6 @@
 version: 1
 name: p0-console-kpi-package-performance-report
-description: Sell two packages and verify package_performance exposes identity attributes, plan status, sold count, settled revenue, data used, and descending revenue order through GraphQL.
+description: Sell two packages and verify package_performance exposes identity attributes, the plan active flag, sold count, settled revenue in cents, data used, and descending revenue order through GraphQL.
 seed: 4709
 suite: p0
 priority: p0
@@ -50,7 +50,7 @@
         package: large_plan
         other_package: small_plan
         expected: before
-        status: active
+        active: true
       - type: performance_report_cell
         report: package_performance
         span: daily
@@ -64,7 +64,9 @@
         networks: net-001
         package: large_plan
         column: revenue
-        expected_value: 7.00
+        # package_performance revenue cells are minor units (unit: cents).
+        # 7.00 USD x 1 sale = 700.
+        expected_value: 700
         tolerance: 0.001
       - type: performance_report_cell
         report: package_performance
