import { Typography } from "@mui/material";
import { CenterContainer } from "../styles/welcome";
const SignUpForm = () => {
    return (
        <CenterContainer>
            <Typography variant="h2">signUp form component</Typography>
        </CenterContainer>
    );
};

export default SignUpForm;
