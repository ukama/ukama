import { Box } from "@mui/material";
import { StatusCard } from "../../components";

const Home = () => {
    return (
        <Box>
            <StatusCard />
        </Box>
    );
};

export default Home;
