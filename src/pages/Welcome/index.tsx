import { Typography } from "@mui/material";
import { CenterContainer } from "../../styles/welcome";
const Dashboard = () => {
    return (
        <CenterContainer>
            <Typography variant="h2">Welcome to Ukama Dashboard</Typography>
        </CenterContainer>
    );
};

export default Dashboard;
