import colors from "./colors";

const themePalette = {
    text: {
        primary: colors.black,
    },
    mode: "dark",
    primary: {
        main: colors.primary,
    },
};

export default themePalette;
