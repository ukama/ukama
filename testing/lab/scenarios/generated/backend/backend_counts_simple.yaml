version: 1
name: backend_counts_simple
seed: 2142771702
suite: generated
priority: p1
tags: [generated, backend, backend, counts, simple]
status: active
generated: true
entity: backend
action: check

# family: backend
# case: backend_counts_simple
# topology: simple
# generated_status: active

provider:
  type: virtual

world:
  networks: 1
  sites_per_network: 1
  nodes_per_site:
    tower: 1
    amplifier: 1
    controller: 1
  ues_per_site: 1

packages:
  - ref: pkg_001
    name: Data Package 1
    data_mb: 1024
    duration_days: 1
    amount: 1.00
    assign_percent: 100

setup:
  create_via_bff:
    - networks
    - sites
    - nodes
    - node_site_links
    - packages
    - subscribers
    - sims

runtime:
  start: [nodes]
  wait: [nodes_ready]

phases:
  - name: action
    events:
      - type: check
    checks:
      - type: backend_count
        target: networks
        expected: from_world
      - type: backend_count
        target: sites
        expected: from_world
      - type: backend_count
        target: nodes
        expected: from_world
      - type: backend_count
        target: sims
        expected: from_world

final_checks:
  - type: balance_non_negative
    ues: all
