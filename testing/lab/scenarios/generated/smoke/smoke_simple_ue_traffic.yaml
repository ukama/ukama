version: 1
name: smoke_simple_ue_traffic
seed: 1013481492
suite: generated
priority: p0
tags: [generated, smoke, smoke, runtime, usage]
status: active
generated: true
entity: smoke
action: traffic

# family: smoke
# case: smoke_simple_ue_traffic
# topology: simple
# generated_status: active

provider:
  type: virtual

world:
  networks: 1
  sites_per_network: 1
  nodes_per_site:
    tower: 1
    amplifier: 1
    controller: 1
  ues_per_site: 1

packages:
  - ref: pkg_001
    name: Data Package 1
    data_mb: 1024
    duration_days: 1
    amount: 1.00
    assign_percent: 100

setup:
  create_via_bff:
    - networks
    - sites
    - nodes
    - node_site_links
    - packages
    - subscribers
    - sims

runtime:
  start: [nodes, ues]
  wait: [nodes_ready, ues_attached]

phases:
  - name: action
    events:
      - type: traffic
        ues: all
        amount_mb: 1024
    checks:
      - type: ue_attached
        ues: all
      - type: usage_per_sim
        ues: all
        expected: from_model
        tolerance_percent: 2
      - type: balance_non_negative
        ues: all

final_checks:
  - type: balance_non_negative
    ues: all
