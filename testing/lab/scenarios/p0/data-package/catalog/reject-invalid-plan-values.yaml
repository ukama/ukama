version: 1
name: p0-reject-invalid-plan-values
description: Verify BFF rejects package creation requests with invalid allowance, duration, price, or currency without leaving a catalog record.
seed: 4102
suite: p0
priority: p0
tags: [virtual, data-package, catalog, negative]
status: wip
provider:
  type: virtual
world:
  networks: 1
  sites_per_network: 1
  nodes_per_site:
    tower: 1
    amplifier: 1
    controller: 1
  ues_per_site: 0
setup:
  create_via_bff:
    - networks
    - sites
    - nodes
    - node_site_links
runtime:
  start: [nodes]
  wait: [nodes_ready]
phases:
  - name: reject_invalid_values
    # Requires the planned create_package event so each invalid mutation can
    # be asserted independently without making scenario setup invalid.
