version: 1
name: p0-organization-and-network-plan-scope
description: Verify an organization plan is visible to both networks while each network plan remains isolated to its owning network.
seed: 4103
suite: p0
priority: p0
tags: [virtual, data-package, catalog, scope, multi-network]
status: wip
provider:
  type: virtual
world:
  networks: 2
  sites_per_network: 1
  nodes_per_site:
    tower: 1
    amplifier: 1
    controller: 1
  ues_per_site: 0
setup:
  create_via_bff:
    - networks
    - sites
    - nodes
    - node_site_links
runtime:
  start: [nodes]
  wait: [nodes_ready]
phases:
  - name: verify_plan_scope
    # Requires organization-scoped package creation in the lab world model.
