version: 1
name: p0-initial-payment-failure-is-atomic
description: Verify a forced initial-payment failure leaves no allocated usable SIM, entitlement, sale, or revenue contribution.
seed: 4203
suite: p0
priority: p0
tags: [virtual, data-package, allocation, payment-failure, atomicity]
status: wip
provider:
  type: virtual
world:
  networks: 1
  sites_per_network: 1
  nodes_per_site:
    tower: 1
    amplifier: 1
    controller: 1
  ues_per_site: 0
setup:
  create_via_bff:
    - networks
    - sites
    - nodes
    - node_site_links
runtime:
  start: [nodes]
  wait: [nodes_ready]
phases:
  - name: fail_initial_payment
    # Requires the virtual payment-provider failpoint selected before setup.
