version: 1
name: p0-traffic-before-expiration-remains-allowed
description: Verify a minute-based package remains usable immediately before its expiration boundary.
seed: 4507
suite: p0
priority: p0
tags: [virtual, data-package, policy, before-expiration]
status: wip
provider:
  type: virtual
world:
  networks: 1
  sites_per_network: 1
  nodes_per_site:
    tower: 1
    amplifier: 1
    controller: 1
  ues_per_site: 0
setup:
  create_via_bff:
    - networks
    - sites
    - nodes
    - node_site_links
runtime:
  start: [nodes]
  wait: [nodes_ready]
phases:
  - name: verify_contract
