version: 1
name: p0-package-transition-has-no-gap
description: Verify traffic continues without an unintended policy outage when an ended package hands service to its queued successor.
seed: 4510
suite: p0
priority: p0
tags: [virtual, data-package, policy, transition, no-gap]
status: wip
provider:
  type: virtual
world:
  networks: 1
  sites_per_network: 1
  nodes_per_site:
    tower: 1
    amplifier: 1
    controller: 1
  ues_per_site: 0
setup:
  create_via_bff:
    - networks
    - sites
    - nodes
    - node_site_links
runtime:
  start: [nodes]
  wait: [nodes_ready]
phases:
  - name: verify_contract
