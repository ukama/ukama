version: 1
name: p0-queued-package-activates-after-expiration
description: Verify a queued paid package activates automatically when the current minute-based package expires with unused data remaining.
seed: 4502
suite: p0
priority: p0
tags: [virtual, data-package, policy, expiration, transition]
status: wip
provider:
  type: virtual
world:
  networks: 1
  sites_per_network: 1
  nodes_per_site:
    tower: 1
    amplifier: 1
    controller: 1
  ues_per_site: 0
setup:
  create_via_bff:
    - networks
    - sites
    - nodes
    - node_site_links
runtime:
  start: [nodes]
  wait: [nodes_ready]
phases:
  - name: verify_contract
