version: 1
name: p0-exact-usage-boundary
description: Verify the final allowed bytes are accounted once at the exact allowance boundary and subsequent traffic is blocked or charged to the next package.
seed: 4508
suite: p0
priority: p0
tags: [virtual, data-package, policy, usage-boundary]
status: wip
provider:
  type: virtual
world:
  networks: 1
  sites_per_network: 1
  nodes_per_site:
    tower: 1
    amplifier: 1
    controller: 1
  ues_per_site: 0
setup:
  create_via_bff:
    - networks
    - sites
    - nodes
    - node_site_links
runtime:
  start: [nodes]
  wait: [nodes_ready]
phases:
  - name: verify_contract
