version: 1
name: p0-concurrent-purchases-are-serialized
description: Verify concurrent package purchases for one SIM create all and only the requested payments and a deterministic entitlement order.
seed: 4308
suite: p0
priority: p0
tags: [virtual, data-package, purchase, concurrency]
status: wip
provider:
  type: virtual
world:
  networks: 1
  sites_per_network: 1
  nodes_per_site:
    tower: 1
    amplifier: 1
    controller: 1
  ues_per_site: 0
setup:
  create_via_bff:
    - networks
    - sites
    - nodes
    - node_site_links
runtime:
  start: [nodes]
  wait: [nodes_ready]
phases:
  - name: purchase_concurrently
    # Requires parallel purchase execution and a BFF idempotency contract.
