version: 1
name: p0-failed-payment-creates-no-entitlement
description: Verify a deterministically rejected additional payment contributes no sale, revenue, or queued package entitlement.
seed: 4307
suite: p0
priority: p0
tags: [virtual, data-package, purchase, payment-failure]
status: wip
provider:
  type: virtual
world:
  networks: 1
  sites_per_network: 1
  nodes_per_site:
    tower: 1
    amplifier: 1
    controller: 1
  ues_per_site: 0
setup:
  create_via_bff:
    - networks
    - sites
    - nodes
    - node_site_links
runtime:
  start: [nodes]
  wait: [nodes_ready]
phases:
  - name: reject_additional_payment
    # Requires the virtual payment-provider failpoint.
