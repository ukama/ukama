version: 1
name: p0-console-kpi-package-performance-report
description: Verify each package-performance row reports plan identity, sold count, actual revenue, data used, status, and ordering.
seed: 4709
suite: p0
priority: p0
tags: [virtual, console-kpi]
status: wip
provider:
  type: virtual
world:
  networks: 1
  sites_per_network: 1
  nodes_per_site:
    tower: 1
    amplifier: 1
    controller: 1
  ues_per_site: 0
setup:
  create_via_bff:
    - networks
    - sites
    - nodes
    - node_site_links
runtime:
  start: [nodes]
  wait: [nodes_ready]
phases:
  - name: verify_contract
    # Add package setup and performance_report_cell assertions before activation.
