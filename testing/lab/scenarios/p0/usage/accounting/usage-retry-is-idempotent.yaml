version: 1
name: p0-usage-retry-is-idempotent
description: WIP until BFF GraphQL exposes an operation that can replay the same CDR identity; generating a second traffic session would create a new usage fact and would not test ingestion idempotency.
seed: 4517
suite: p0
priority: p0
tags: [virtual, usage, accounting, idempotency, graphql-gap]
status: wip
provider:
  type: virtual
world:
  networks: 1
  sites_per_network: 1
  nodes_per_site:
    tower: 1
    amplifier: 1
    controller: 1
  ues_per_site: 0
setup:
  create_via_bff: [networks, sites, nodes, node_site_links]
runtime:
  start: [nodes]
  wait: [nodes_ready]
phases:
  - name: blocked_by_graphql_contract

