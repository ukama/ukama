version: 1
name: p0-single-session-usage
description: Generate one known traffic session and verify the SIM usage recorded through BFF matches it within accounting tolerance.
seed: 4514
suite: p0
priority: p0
tags: [virtual, usage, accounting, single-session]
status: wip
provider:
  type: virtual
world:
  networks: 1
  sites_per_network: 1
  nodes_per_site:
    tower: 1
    amplifier: 1
    controller: 1
  ues_per_site: 0
setup:
  create_via_bff:
    - networks
    - sites
    - nodes
    - node_site_links
runtime:
  start: [nodes]
  wait: [nodes_ready]
phases:
  - name: verify_contract
