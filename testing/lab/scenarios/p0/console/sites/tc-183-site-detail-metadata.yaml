version: 1
name: p0-tc-183-site-detail-metadata
description: Verify Site detail returns the site name, creation timestamp, non-empty location, network association, latitude, and longitude created for the test.
seed: 5183
suite: p0
priority: p0
tags: [virtual, console, sites, metadata, tc-183]
status: active
provider:
  type: virtual
world:
  networks: 1
  sites_per_network: 1
  nodes_per_site:
    tower: 1
    amplifier: 1
    controller: 1
  ues_per_site: 0
setup:
  create_via_bff: [networks, sites, nodes, node_site_links]
runtime:
  start: [nodes]
  wait: [nodes_ready]

phases:
  - name: verify_site_metadata
    checks:
      - type: entity_fields_equal
        entity: site
        ref: site-001
        view: site_detail
