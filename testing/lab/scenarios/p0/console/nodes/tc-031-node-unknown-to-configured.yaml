version: 1
name: p0-tc-031-node-unknown-to-configured
description: Verify Node Pool reports Unknown before assignment, Nodes List reports Configuring/Offline while readiness is held, and Operational/Online after recovery.
seed: 5031
suite: p0
priority: p0
tags: [virtual, console, nodes, lifecycle, configuring, tc-031]
status: active
provider:
  type: virtual
world:
  networks: 1
  sites_per_network: 1
  nodes_per_site:
    tower: 1
    amplifier: 1
    controller: 1
  ues_per_site: 0
setup:
  create_via_bff: [networks, nodes, node_site_links]
runtime:
  start: [nodes]
  wait: [nodes_ready]

phases:
  - name: verify_unknown
    checks:
      - type: node_status_equals
        view: node_pool
        nodes: all
        connectivity: Online
        state: Unknown
        timeout_seconds: 120
  - name: configure_without_ready
    events:
      - type: configure_sites
        sites: all
        state: hold
    checks:
      - type: node_status_equals
        view: nodes_list
        nodes: all
        connectivity: Offline
        state: Configuring
        timeout_seconds: 180
  - name: restore_ready_nodes
    events:
      - type: reconnect_nodes
        nodes: all
      - type: wait_node_connectivity
        nodes: all
        connectivity: Online
        seconds: 180
    checks:
      - type: node_status_equals
        view: nodes_list
        nodes: all
        connectivity: Online
        state: Operational
        timeout_seconds: 180
