version: 1
name: p0-console-kpi-rollup-retry-idempotency
description: WIP until BFF GraphQL exposes an ingestion or rollup replay mutation; repeatedly reading getKpiValues cannot trigger recomputation and therefore cannot test retry idempotency.
seed: 4723
suite: p0
priority: p0
tags: [virtual, console, analytics, idempotency, graphql-gap]
status: wip
provider:
  type: virtual
world:
  networks: 1
  sites_per_network: 1
  nodes_per_site:
    tower: 1
    amplifier: 1
    controller: 1
  ues_per_site: 0
setup:
  create_via_bff: [networks, sites, nodes, node_site_links]
runtime:
  start: [nodes]
  wait: [nodes_ready]
phases:
  - name: blocked_by_graphql_contract
