version: 1
name: software_update_tower_app_example
description: Verify the example app is healthy at the expected current version on the tower node, update it from the exact tar.gz version published in Hub, and confirm the running target version and app health after the update.
seed: 3837459242
suite: generated
priority: p1
tags: [generated, software_update, software_update, tower]
status: active
generated: true
entity: software_update
action: software_update_tower

# family: software_update
# case: software_update_tower_success
# topology: simple
# generated_status: active

provider:
  type: virtual

world:
  networks: 1
  sites_per_network: 1
  nodes_per_site:
    tower: 1
    amplifier: 1
    controller: 1
  ues_per_site: 0

setup:
  create_via_bff:
    - networks
    - sites
    - nodes
    - node_site_links

runtime:
  start: [nodes]
  wait: [nodes_ready]

phases:
  - name: verify_app_before_update
    checks:
      - type: node_health_ok
        type_selector: tower
        count_per_network: 1
        app: example
      - type: node_version_equals
        type_selector: tower
        count_per_network: 1
        app: example
        version: ${ULAB_SOFTWARE_CURRENT_VERSION}

  - name: action
    events:
      - type: software_update
        type_selector: tower
        count_per_network: 1
        app: example
        tag: ${ULAB_SOFTWARE_TARGET_VERSION}
    checks:
      - type: node_version_equals
        type_selector: tower
        count_per_network: 1
        app: example
        version: ${ULAB_SOFTWARE_TARGET_VERSION}
      - type: node_health_ok
        type_selector: tower
        count_per_network: 1
        app: example
