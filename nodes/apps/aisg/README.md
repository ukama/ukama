# AISG Stack

This directory contains the Ukama AISG node stack.

- `aisgd`: northbound Ukama app
- `ctrl`: controller daemon
- `emu`: controller-contract emulator

V1 is single-device only and intentionally excludes daisy-chain and
firmware download.

