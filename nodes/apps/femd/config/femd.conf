# FEM Daemon Configuration File
# Simple key=value format

# Service configuration
service_name=femd
service_port=8080

# Logging configuration
log_level=INFO

# Future configuration options will be added here as we expand features