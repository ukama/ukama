# Config file for bootstrap

[config]

	noded-host       = "localhost"
	mesh-config      = "/conf/mesh_config.toml"
	remote-ip-file   = "/conf/ip_file"
	bootstrap-server = "localhost:8070"

#nothing here.
