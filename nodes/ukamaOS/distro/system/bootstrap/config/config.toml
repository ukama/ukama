# Config file for bootstrap

[config]

	noded-host       = "localhost"
	noded-port       = "8095"
	mesh-config      = "/conf/bootstrap/config.toml"
	remote-ip-file   = "/conf/bootstrap/ip_file"
	bootstrap-server = "localhost:8070"

#nothing here.
