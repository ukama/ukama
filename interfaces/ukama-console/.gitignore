# dependencies
node_modules/

# next.js
.next/
out/
next-env.d.ts

# builds
build/
*.tsbuildinfo

# dev review logs (Claude reads these; never commit)
.logs/*
!.logs/.gitkeep

# env
.env
.env*.local

# local planning doc (kept out of version control)
ukama-console-BUILD-PLAN.md

# misc
.DS_Store
*.pem
npm-debug.log*
pnpm-debug.log*

# graphql codegen output is committed (CI has no BFF to introspect): the
# *.generated.ts operation files import ./types, so types.ts must be tracked
# too. Keep all codegen output in sync via `pnpm codegen` before committing.
ukama-console-API-PLAN.md
ukama-console-CONTEXT.md
