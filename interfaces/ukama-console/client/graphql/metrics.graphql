# Metrics Query

query GetLatestMetric($data: GetLatestMetricInput!) {
  getLatestMetric(data: $data) {
    env
    nodeid
    type
    value
  }
}

query GetMetricRange($data: GetMetricRangeInput!) {
  getMetricRange(data: $data) {
    env
    nodeid
    type
    values
  }
}

subscription GetMetricRangeSub(
  $nodeId: String!
  $orgId: String!
  $type: String!
  $userId: String!
) {
  getMetricRangeSub(
    nodeId: $nodeId
    orgId: $orgId
    type: $type
    userId: $userId
  ) {
    env
    nodeid
    type
    values
  }
}
