'use client';

import { CenterContainer } from '@/styles/global';
import { CircularProgress } from '@mui/material';

const Page = () => {
  return (
    <CenterContainer>
      <CircularProgress />
    </CenterContainer>
  );
};

export default Page;
